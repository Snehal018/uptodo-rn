import { AppColors } from './color';
import { fontSize, fontTypes, fontWeight } from './font';
import {
  SCREEN_HEIGHT,
  SCREEN_WIDTH,
  STANDARD_HEIGHT,
  STANDARD_WIDTH,
  VERTICAL_BASE_SCALE,
} from './metrics';

export {
  AppColors,
  fontSize,
  fontTypes,
  fontWeight,
  SCREEN_HEIGHT,
  SCREEN_WIDTH,
  STANDARD_HEIGHT,
  STANDARD_WIDTH,
  VERTICAL_BASE_SCALE,
};
